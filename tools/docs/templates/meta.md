# Meta Endpoints

- `GET /api/image-builder-composer/v2/ready` - Health check endpoint
- `GET /api/image-builder-composer/v2/openapi.json` - OpenAPI specification

## Health Check Endpoint

Example curl request:

```bash
!INCLUDE "examples/api/meta/ready.sh"
```

Example HTTP request:

```http
!INCLUDE "examples/api/meta/ready.http"
```

## OpenAPI Specification Endpoint

Example curl request:

```bash
!INCLUDE "examples/api/meta/openapi.sh"
```

Example HTTP request:

```http
!INCLUDE "examples/api/meta/openapi.http"
```
